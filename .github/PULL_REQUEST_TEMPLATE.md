## What changed and why

## How you tested it
- [ ] `dotnet build` passes
- [ ] `dotnet test plugin-tests/plugin-tests.csproj --filter "Category!=RealP2P"` passes
- [ ] Tested in-game if this touches sync/connection behavior

## Anything risky or worth flagging?
